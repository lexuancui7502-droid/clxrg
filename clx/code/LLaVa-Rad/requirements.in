# 顶层依赖：只写你关心的库，范围/上限可控
# PyTorch 与 CUDA 建议先用你当前环境中的精确版本（见做法 B 第 3 步的替换办法）
torch
torchvision
torchaudio

transformers>=4.40,<4.46
accelerate>=0.25,<0.34
tokenizers>=0.15,<0.20
datasets>=2.16,<2.21
peft>=0.7,<0.12
open-clip-torch>=2.20,<2.25

# 评测相关
evaluate>=0.4,<0.7
rouge-score>=0.1,<0.2
scikit-learn>=1.3,<1.6
numpy>=1.24,<2

# RadGraph / AllenNLP（你之前踩过命名空间坑，这里锁定主版本）
radgraph==0.0.9
allennlp==2.10.1
